# ── Glow with Azmir backend — environment ──────────────────────────────────
# Copy this file to ".env" and adjust. The database is Firebase Firestore;
# credentials come from the service-account key file (NOT from here).

# Port the API listens on.
PORT=4000

# Comma-separated list of allowed frontend origins for CORS.
# Use * to allow all (fine for internal use). In production set your panel URL.
CORS_ORIGIN=*

# Public base URL where this backend is reachable (used to build upload URLs).
# Local: http://localhost:4000   Production: https://your-backend.example.com
PUBLIC_URL=http://localhost:4000

# OPTIONAL light protection (no login UI). If set, every write request must
# send header  x-api-key: <this value>. Leave empty to keep the API open.
# The frontend reads this from VITE_API_KEY.
API_KEY=

# ── Firebase ───────────────────────────────────────────────────────────────
# Firebase Admin authenticates with a service-account JSON key.
# Place it at the backend root as  serviceAccountKey.json  (gitignored).
# Set FIREBASE_SERVICE_ACCOUNT only to point at a different path (e.g. on a host).
FIREBASE_SERVICE_ACCOUNT=
